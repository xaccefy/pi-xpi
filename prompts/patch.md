---
description: Automated patching pipeline - patch-writer refactors the code to fix the vulnerability and runs validations
---
Call the Agent tool to launch the "patch-writer" subagent:

Use the patch-writer agent to patch the codebase and resolve the vulnerability report or exploit output described in: $@
